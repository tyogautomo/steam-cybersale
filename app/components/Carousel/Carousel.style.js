import { StyleSheet } from 'react-native';

import { colors } from '../../themes/colors';

const styles = StyleSheet.create({
   container: {
      marginVertical: 10
   },
   title: {
      marginHorizontal: 10,
      fontSize: 14,
      fontStyle: 'italic',
      color: colors.theme5
   }
});

export { styles };
